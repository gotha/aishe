apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aishe.fullname" . }}-test-connection"
  labels:
    {{- include "aishe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aishe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
