apiVersion: v1
kind: Service
metadata:
  name: {{ include "aishe.fullname" . }}
  labels:
    {{- include "aishe.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "aishe.selectorLabels" . | nindent 4 }}
